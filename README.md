# INew OpenBao Control Agent Installer

Автор: INew

В v1.0.7 исправлен формат обмена с аутентифицированными root-generation API OpenBao
2.6.1: JSON-обёртка `data` и Base64 без padding. Проверен полный изолированный
сценарий с двумя долями и отзывом временного root. Первоначальное обновление —
только DEV; рабочую Windows-сборку с исправлением аудита v1.0.6 менять не нужно.

Начиная с v1.0.6 доступен отдельный план `operator-audit-read-repair-v1` только для
DEV: добавление `sudo` на точном `sys/audit` в `platform-operator`. Процедура требует
обновлённого Windows Manager, явного подтверждения, двух Unseal-долей и резервного
Raft-снимка. Обычный вход по-прежнему выполняется через Userpass/TOTP; постоянный
root не нужен. Ограничения и восстановление описаны в `RELEASE.md`.

Публичный установщик Linux-компонента, который связывает OpenBao Manager с локальным OpenBao через защищённый HTTPS-контур. В Git находятся только установщик, проверяющие скрипты и безопасные шаблоны. Исполняемый файл Agent публикуется отдельно в GitHub Releases.

Репозиторий: [InewAlex/inew-openbao-control-agent-installer](https://github.com/InewAlex/inew-openbao-control-agent-installer).

## Что автоматизирует установщик

- устанавливает отдельный экземпляр для `dev` или `prod`;
- создаёт системного пользователя и закрытые каталоги;
- локально создаёт отдельный device CA выбранной среды;
- формирует точный `agent.json` без паролей и TOTP;
- настраивает и проверяет systemd, Nginx и logrotate;
- проверяет конфигурацию Agent до запуска;
- после запуска ожидает готовность локального health endpoint до 30 секунд;
- сохраняет существующие файлы и автоматически откатывает их при ошибке;
- при первой установке показывает одноразовый код привязки к OpenBao Manager.

Сайты и Laravel-проекты могут находиться на других серверах. Agent устанавливается на сервере, где OpenBao доступен через loopback-адрес `127.0.0.1`.

## Поддерживаемые системы

- Ubuntu Server 22.04 и 24.04;
- Debian 12 и 13;
- архитектура `x86_64`;
- systemd, Nginx, OpenSSL, curl и стандартные GNU-утилиты;
- OpenBao 2.6.x.

## Простая установка

1. Откройте раздел **Releases** этого репозитория.
2. Скачайте asset `inew-openbao-control-agent-v<версия>-linux-x64.tar.gz`.
3. Сверьте SHA-256 архива со значением, опубликованным владельцем в описании Release.
4. Создайте временный рабочий каталог, распакуйте архив и откройте полученный каталог.
5. Запустите установщик из распакованного каталога:

```bash
sudo ./install.sh
```

Установщик задаст девять понятных вопросов. Пароли OpenBao, TOTP, root token и секреты проектов он не запрашивает и не сохраняет.

Не запускайте установку конструкцией `curl | bash`: сначала скачайте и проверьте архив.

Распакованный каталог, например `/tmp/tmp.XXXXXX/inew-openbao-control-agent-v<версия>-linux-x64`, является только временным местом запуска. Удаление этого каталога после успешной установки не влияет на службу.

## Постоянные пути установки

Установщик сам переносит файлы в системные каталоги. Не копируйте бинарный файл вручную и не запускайте службу из `/tmp`.

| Назначение | Постоянный путь |
|---|---|
| Версия бинарного файла | `/opt/inew-openbao-control-agent/releases/<версия>/inew-openbao-control-agent` |
| Активная версия DEV | `/opt/inew-openbao-control-agent/current-dev` |
| Активная версия PROD | `/opt/inew-openbao-control-agent/current-prod` |
| Конфигурация среды | `/etc/inew-openbao-control-agent/<env>/agent.json` |
| Состояние среды | `/var/lib/inew-openbao-control-agent/<env>` |
| Аудит среды | `/var/log/inew-openbao-control-agent/<env>` |
| Резервные копии | `/var/backups/inew-openbao-control-agent` |
| Служба systemd | `inew-openbao-control-agent@<env>.service` |

`current-dev` и `current-prod` — независимые символические ссылки на проверенную версию в каталоге `releases`. Шаблон systemd всегда запускает бинарный файл через соответствующую ссылку `current-<env>`.

## Какие данные понадобятся

| Поле | Что означает |
|---|---|
| Среда | `dev` или `prod`; за один запуск устанавливается одна среда |
| Публичное DNS-имя | HTTPS-адрес, по которому Windows-программа увидит Agent |
| Порт Agent | Локальный порт; по умолчанию 9120 для DEV и 9130 для PROD |
| Порт OpenBao | Локальный порт; по умолчанию 8200 для DEV и 8300 для PROD |
| TLS-сертификат | Уже существующий сертификат публичного DNS-имени |
| TLS-ключ | Уже существующий закрытый ключ этого сертификата |
| Policies | Разрешённые policy учётной записи оператора OpenBao |
| Entity IDs | Разрешённые entity ID операторов OpenBao |
| TTL | Минимальный оставшийся срок действия токена, обычно 300 секунд |

Policy и entity ID не являются паролями, но описывают внутреннюю структуру доступа. Не публикуйте заполненный файл ответов.

## Совместимость оператора с OpenBao 2.6.1

Начиная с v1.0.4 установщик разрешает штатные orphan service-токены Userpass. Orphan означает отсутствие родительского токена, а не права root. Root-токены запрещены; обязательны разрешённые policies и entity ID, тип service, достаточный оставшийся TTL и сертификат привязанного устройства.

Учитываются token policies и identity policies. Значение `meta.environment_id` от Userpass не требуется: экземпляр Agent выбирает OpenBao только из своей локальной конфигурации. Токены с ограниченным числом использований не поддерживаются.

С v1.0.5 поддерживаются IP-bound токены: Agent получает исходный адрес от локального nginx после проверки устройства, передаёт его OpenBao и дополнительно сверяет с bound_cidrs. Пароль и TOTP по-прежнему вводятся только в локальном Windows-приложении.

Перед обновлением администратор проверяет передачу исходного IP на всём маршруте. OpenBao должен доверять X-Forwarded-For только от проверенного локального прокси. Установщик не меняет listener OpenBao, настройки пользователя, token_bound_cidrs или token_strictly_bind_ip. Подробный порядок — в [RELEASE.md](RELEASE.md#проверка-ip-привязки-перед-обновлением).

Для проверки оператора нужны `read` на `auth/token/lookup-self` и `update` на `auth/token/revoke-self`. Чтение платформы также использует `sys/seal-status`, `sys/mounts`, `sys/auth`, `sys/audit`, список и чтение `identity/mfa/login-enforcement`, чтение `identity/mfa/method/<id>`. Администратор проверяет необходимые capabilities, включая `sudo` для защищённых системных API; это не инструкция выдавать оператору полный доступ к `sys/*`.

Посторонний TOTP-метод не подтверждает MFA для выбранного оператора. При правилах только по группам, членство в которых не проверено, Agent показывает неопределённое состояние. HTTP 403 сам по себе не доказывает ошибку policy. Если `/sys/mfa/validate` отклонён, новая сессия не создана: не запускайте последующий lookup как будто вход завершился.

Windows OpenBao Manager остаётся локальным и не входит ни в этот Git-репозиторий, ни в Linux-архив. Для исправления IP-привязки v1.0.5 требуется соответствующая локальная сборка Manager и новый бинарник Agent; одного обновления `install.sh` в Git недостаточно.

## Установка через закрытый файл ответов

Для повторяемой установки создайте на сервере `/root/agent-install.env`, принадлежащий `root` с правами `0600`:

```dotenv
INEW_ENVIRONMENT=dev
INEW_PUBLIC_HOST=agent-dev.example.com
INEW_AGENT_PORT=9120
INEW_OPENBAO_PORT=8200
INEW_TLS_CERTIFICATE_PATH=/etc/letsencrypt/live/agent-dev.example.com/fullchain.pem
INEW_TLS_PRIVATE_KEY_PATH=/etc/letsencrypt/live/agent-dev.example.com/privkey.pem
INEW_OPERATOR_POLICIES=replace-with-operator-policy
INEW_OPERATOR_ENTITY_IDS=replace-with-operator-entity-id
INEW_MIN_OPERATOR_TTL_SECONDS=300
```

```bash
sudo chmod 0600 /root/agent-install.env
sudo ./install.sh --answers /root/agent-install.env
```

Файл разбирается как данные, а не исполняется как shell-скрипт. Не добавляйте в него пароли, TOTP, токены, SecretID или секреты проектов.

## После установки

При первой установке скопируйте показанный одноразовый код в OpenBao Manager и завершите привязку соответствующей среды. Для второго контура повторно запустите установщик с другими DNS-именем, портами, policy и entity ID.

Диагностика, обновление и ручное создание нового pairing-кода описаны в [RELEASE.md](RELEASE.md). Правила безопасной публикации и сообщения об уязвимостях — в [SECURITY.md](SECURITY.md).

## Ограничение версии 1

Целостность бинарного файла проверяется SHA-256. В первой версии нет цифровой подписи издателя и автоматического сетевого обновления. SHA-256 внутри архива обнаруживает повреждение, но сам по себе не доказывает авторство архива.
